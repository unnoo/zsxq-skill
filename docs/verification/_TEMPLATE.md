<!--
验证报告模板。复制本文件为 docs/verification/<id>.md 后填写。
<id> = reference 文件名去扩展名（如 topic-attached-columns）或场景 id（如 archive-topics-to-column）。
配套原始日志：docs/verification/logs/<id>.log（用 logs/_TEMPLATE.log）。
填写规范见 ../skill-dev-workflow.md 的「④ 验证」。删除本注释块再提交。
-->

# 验证报告：<id>

| 项 | 值 |
|----|----|
| 被测文档 | `references/<...>.md`（操作）或 `references/scenarios/<id>.md`（场景） |
| 分类 | 读 / 写 · 操作 / 场景 |
| zsxq-cli 版本 | vX.Y.Z |
| 测试日期 | YYYY-MM-DD |
| 测试对象 | 星球名 + group_id / 主题 id / 笔记 id（写操作务必写清在哪测的） |
| 原始日志 | [`logs/<id>.log`](logs/<id>.log) |
| 授权 | 只读填「不涉及写入」；写操作填用户一次性授权的来源与范围 |

## 测试用例

> 操作：逐命令覆盖正常路径、每个关键参数、每条特有错误、失败语义。
> 场景：主干跑通一次 + 每条「分支与停止条件」各一个用例。
> 「实际」列摘录真实输出关键片段，完整输出见日志对应段。

| # | 用例（意图） | 执行的命令 | 预期 | 实际（真实输出摘录） | 结论 |
|---|------------|-----------|------|---------------------|------|
| 1 | 正常路径 | `zsxq-cli ...` | ... | ... | ✅ |
| 2 | 关键参数 X | `zsxq-cli ...` | ... | ... | ✅ |
| 3 | 特有错误 Y | `zsxq-cli ...` | 返回 Y 错误 | ... | ✅ |

## 实测校准了哪些文档假设

> 实现阶段基于理解写的初稿，被真实输出推翻 / 修正的地方逐条记；没有则写「无，文档与实测一致」。

- 例：set 返回 `resp_data:{}` 而非回显列表 → 校准 `<file>` 的「输出」节
- 例：实测存在 429 限流 → 补「错误说明」+ 场景加 ~2 秒间隔

## 安全测试策略（写操作必填，只读写「不涉及写入」）

- **策略**：dry-run / 自复原（net-zero）/ 测试星球
- **如何保证净零变更**：（自复原须写清读原值→写测试值→复原的字段与步骤）
- **复原验证**：复原后再读一次的结果（证明已还原；对应日志段落）

## 未覆盖 / 已知风险

- 例：100 条 / 专栏上限的超限失败分支未实测（无接近上限的测试专栏）
- 例：写路径因用户约束未实测

## 结论

**通过 / 有条件通过（注明条件，如「写路径未实测」）/ 失败**

一句话总结实测结果与文档可信度。
